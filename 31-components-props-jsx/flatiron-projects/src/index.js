import React from "react";
import ReactDOM from "react-dom";
import "./index.css";
import App from "./App";

ReactDOM.render(
  // what we want to display (our entire application)
  <App />,
  // where we want to display it
  document.getElementById("root")
);

// webpack
